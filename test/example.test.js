// IMPORT MODULES under test here:
//import { getProductById } from '../utils/utils.js';

const test = QUnit.test;

//test(`getProductById should return the first product object id: 1`, function(assert) {
test(`test the test function true and true should pass`, function(assert) {
    //Arrange
    // Set up your parameters and expectations
    // const firstProductId = 1;
    // const firstProduct = {
    //     id: 1,
    //     name: 'bag',
    //     imageurl: '../assets/bag.jpg',
    // };

    //Act 
    // Call the function you're testing and set the result to a const
    // const testFirstProduct = getProductById(firstProductId);

    //Assert
    // Make assertions about what is expected valid result
    //assert.deepequal(testFirstProduct, firstProduct);
    assert.equal(true, true);
});
